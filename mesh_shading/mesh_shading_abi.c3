// Generated by gpu_shaders from the 'mesh' ABI schema - do not edit.
module mesh_shading;

import gpu;

const uint COLUMNS = 8;
const uint ROWS = 6;
const uint MESHLETS_PER_TASK = 4;
const uint HEX_VERTICES = 7;
const uint HEX_TRIANGLES = 6;

struct MeshRoot {
    uint  first_meshlet;
    uint  meshlet_count;
    float time;
    float aspect;
}
$assert MeshRoot::size == 16;
$assert $reflect(MeshRoot.first_meshlet).offset == 0;
$assert $reflect(MeshRoot.meshlet_count).offset == 4;
$assert $reflect(MeshRoot.time).offset == 8;
$assert $reflect(MeshRoot.aspect).offset == 12;

struct FragRoot {
    Vec4f tint;
}
$assert FragRoot::size == 16;
$assert $reflect(FragRoot.tint).offset == 0;
